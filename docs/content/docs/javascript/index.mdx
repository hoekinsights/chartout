---
title: Installation
---

Install chartout and embed the widget in a web app.

## Installation

```bash
npm install chartout
```

For React apps, also install peer dependencies:

```bash
npm install chartout react react-dom
```

## Quick start

```ts
import { createChartoutModel } from 'chartout'
import { ChartoutWidget } from 'chartout/react'
import { openWithViz } from 'chartout/store'

const model = createChartoutModel({})

<ChartoutWidget model={model} style={{ width: '100%' }} />

await openWithViz(model, svgElement, 'My chart title')
```

## Next steps

- [Integrations](/docs/javascript/integrations): React, Vue, Svelte, exports, SSR
- [Example usage](/docs/javascript/example-usage): `openWithViz`, `openWithItem`, `openWithCart`
- [API reference](/docs/javascript/api-reference)
